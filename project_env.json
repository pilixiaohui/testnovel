{
  "cli": "codex",
  "commands": {
    "install": "cd project/backend && ${VENV_PYTHON:-/home/agent/.venv/bin/python} -m pip install -e '.[dev]' -q && cd ../frontend && npm install",
    "test": "cd project/backend && ${VENV_PYTHON:-/home/agent/.venv/bin/python} -m pytest tests/ -q && cd ../frontend && npx vitest run"
  },
  "test_fast_sample_rate": 0.1,
  "test_timeout": 120,
  "test_timeout_fast": 60,
  "max_fix_iterations": 3,
  "baseline_on_session_start": true
}
