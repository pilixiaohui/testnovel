{
  "cli": "codex",
  "commands": {
    "install": "cd project/backend && python3 -m pip install -e '.[dev]' -q && cd ../frontend && npm install",
    "test": "cd project/backend && python3 -m pytest tests/ -q && cd ../frontend && npx vitest run",
    "ci": "cd project/backend && python3 -m pytest tests/ -q"
  },
  "test_fast_sample_rate": 0.1,
  "test_timeout": 120,
  "test_timeout_fast": 60,
  "max_fix_iterations": 3,
  "baseline_on_session_start": true
}
