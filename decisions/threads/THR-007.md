---
id: THR-007
topic: Task TASK-093: [planning] F001 是否需要新增 consolidated change（去重/收敛）确认
related_features: []
status: open
started_at: 2026-02-17T18:12:32+00:00
feishu_message_id: om_x100b567eb7ddd4a4b3612d59c551c2b
---

## 对话记录

[2026-02-17 18:12 agent/monitor]
[planning] F001 是否需要新增 consolidated change（去重/收敛）确认

## 背景

仓库中 F001 已存在（`features/pending/F001.md`），且其验收口径已被拆解到两处已通过 validate 的 OpenSpec 变更：

- `openspec/changes/e2e-test-refactor/specs/e2e-harness/spec.md`（高层 E2E harness 要求）
- `openspec/changes/deterministic-harness-baseline/specs/deterministic-harness-baseline/spec.md`（deterministic profile/trace/隔离/fail-fast 细化）

当前存在两种推进方式：

A) **维持现状（推荐）**：继续使用既有 changes + 已生成的 tasks/available 执行实现，不再新增 change。
B) **新增 consolidated change**：创建并提交一个聚焦 F001 的新 change（例如 `f001-deterministic-e2e-harness`），并决定是否要调整/去重既有 features/tasks 以避免重复。

## 需要用户确认

1) 选择推进方式：A 维持现状，还是 B 新增 consolidated change？
2) 若选择 B：
   - 是否需要把 F001 的 `source` 指向 consolidated change 的 spec？
   - 既有的 `deterministic-harness-baseline`（及 F084-F087）如何处理：保留不变还是标记为 superseded/调整依赖？

## 请按以下格式回复

- approach: <A | B>
- f001 source (if B): <指向 consolidated spec | 不调整>
- existing baseline (if B): <保留不变 | 标记 superseded | 调整依赖（请说明）>
